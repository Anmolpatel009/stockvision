[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "pip install yfinance pandas numpy"
]

[phases.start]
cmd = "node dashboard/server.js"